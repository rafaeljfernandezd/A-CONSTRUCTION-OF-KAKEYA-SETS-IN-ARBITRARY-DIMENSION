These are the minimum packages required for the notebook to run:
numpy==2.3.1,
plotly==6.7.0,
matplotlib==3.10.3,
scipy==1.16.0

The file runs without need to install any packages if it is run in Google Colab, where it was originally created.
